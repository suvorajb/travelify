package com.app.opnshft.travelify.model;


//@SuppressWarnings("serial")
public class GooglePlacesDemoServlet{
	/*public void doGet(HttpServletRequest req, HttpServletResponse resp)
			throws IOException {
		resp.setContentType("text/plain");
		resp.getWriter().println("Hello, world");
		GooglePlacesSvcs svc = new GooglePlacesSvcs();
		PlacesResult rslt = svc.search("restaurants", "44.8041322", "-93.1668858");
		//System.out.println( result.getStatus() );
		resp.getWriter().println(rslt.getStatus());
				for ( Place place : rslt )
					resp.getWriter().println(place.getName() + " " + place.getGeometry().getLocation());
				
	}*/
}
